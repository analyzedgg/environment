frontend:
  build: ./frontend
  container_name: frontend
  ports:
   - "80:80"

backend:
  build: ./backend
  container_name: backend
  links:
    - couchdb
  ports:
   - "8080:8080"

couchdb:
  build: ./couchdb
  container_name: couchdb
  ports:
   - "5984:5984"
